# agentrs 子智能体 —— Task 级迭代方案与规模估算

> 把 `agentrs-子智能体技术架构设计.md` 拆成可指派、可验收、可独立回滚的 task，并给出行数与人周估算。
> 架构决策不在本文重复，每个 task 标注对应的设计章节。

- 文档版本：v1.0
- 编写日期：2026-09-07
- 代码基线：`agentrs` @ `6dbf5116e4`
- 行数口径：**物理行**（含空行与注释），实现与测试分开计——与 `uworker-规模估算与排期校准报告.md` 一致
- 费率口径：**1,175 行/人周**（该报告为 AgentRS 选定的值，取自 aionrs）

---

## 目录

1. [估算方法与校准依据](#一估算方法与校准依据)
2. [Task 全景](#二task-全景)
3. [阶段 0：止血](#三阶段-0止血)
4. [阶段 1：取消传播与可寻址](#四阶段-1取消传播与可寻址)
5. [阶段 2：上下文与用量](#五阶段-2上下文与用量)
6. [阶段 3：配置化预算](#六阶段-3配置化预算)
7. [阶段 4：子会话与续跑](#七阶段-4子会话与续跑)
8. [阶段 5：工具投影与隔离](#八阶段-5工具投影与隔离)
9. [阶段 6：Agent 定义与预制](#九阶段-6agent-定义与预制)
10. [总规模与排期](#十总规模与排期)
11. [风险与估算置信度](#十一风险与估算置信度)

---

## 一、估算方法与校准依据

### 1.1 测试比：用本仓真实数据，不用经验值

对 `agentrs` 现有代码实测（实现行 = `src/**/*.rs` 去除 `*_test.rs`；测试行 = `*_test.rs` + `tests/`）：

| crate | 实现行 | 测试行 | 测试比 |
|---|---|---|---|
| `agentrs-agent` | 8,847 | 19,835 | **2.24** |
| `agentrs-tools` | 5,964 | 8,727 | 1.46 |
| `agentrs-types` | 533 | 916 | 1.71 |
| `agentrs-config` | 3,534 | 4,234 | 1.19 |

模块级样本（说明离散度很大，需按模块性质分别取值）：

| 模块 | 实现 | 测试 | 比 | 性质 |
|---|---|---|---|---|
| `tool_policy.rs` | 35 | 41 | 1.17 | 纯函数 |
| `spawner.rs` | 309 | 265 | 0.86 | 装配为主 |
| `session.rs` | 598 | 616 | 1.03 | 状态 + IO |
| `todo_reminder.rs` | 217 | 324 | 1.49 | 状态机 |
| `task/`（整模块） | 1,059 | 607 | 0.57 | 新写、测试尚未补齐 |
| `context.rs` | 366 | 1,041 | **2.84** | 提示词组装，分支密集 |

**本文取值规则**：

| 模块性质 | 测试比 | 依据 |
|---|---|---|
| 纯函数 / 配置结构 | **1.2** | `tool_policy` / `agentrs-config` |
| 装配 / 接线 | **1.0** | `spawner` |
| 状态机 / 并发 / 生命周期 | **1.8** | 高于 `todo_reminder`，因涉及并发与取消 |
| 提示词与上下文组装 | **2.5** | `context.rs` 实测 2.84，取略保守 |
| 协议 / 序列化 | **1.2** | `agentrs-config` |

> 直接套 `agentrs-agent` 的 2.24 会高估：那个数字被 `engine_test.rs`（3,360 行）与 `context_test.rs`（1,041 行）拉高，而本次多数 task 是装配与状态机，不是提示词组装。

### 1.2 修改行如何计

改动既有代码时，**改一行计一行**（删除计 0.5 行）。重构类 task 另标"触及行数"以反映 review 成本，但不计入产能换算——否则 `Arc<dyn Tool>` 那种机械改动会严重虚高。

### 1.3 不计入的部分

文档、CHANGELOG、`docs/*.md` 更新不计入行数（约占额外 5–8% 工时，在排期里单独留）。

---

## 二、Task 全景

| 阶段 | Task 数 | 实现行 | 测试行 | 合计 | 人周 |
|---|---|---|---|---|---|
| 0 止血 | 3 | 55 | 130 | 185 | 0.16 |
| 1 取消传播与可寻址 | 6 | 690 | 1,090 | 1,780 | 1.51 |
| 2 上下文与用量 | 5 | 400 | 800 | 1,200 | 1.02 |
| 3 配置化预算 | 4 | 330 | 480 | 810 | 0.69 |
| 4 子会话与续跑 | 7 | 1,010 | 1,660 | 2,670 | 2.27 |
| 5 工具投影与隔离 | 5 | 700 | 1,090 | 1,790 | 1.52 |
| 6 Agent 定义与预制 | 6 | 780 | 1,160 | 1,940 | 1.65 |
| **合计** | **36** | **3,965** | **6,410** | **10,375** | **8.83** |

**测试比 1.62**，落在本仓 1.19–2.24 的区间内，偏中位——符合"以装配与状态机为主，少量提示词组装"的构成。

依赖关系（阶段内 task 可并行，阶段间串行）：

```
阶段0 ──► 阶段1 ──► 阶段2 ──► 阶段3
                       │
                       └──► 阶段4 ──► 阶段5 ──► 阶段6
                                              └──► Team 线
```

阶段 3 与阶段 4 可并行（互不触及同一文件）。

---

## 三、阶段 0：止血

**目标**：修掉不需要设计决策的确定性缺陷。无接口变更，可当天合入。

### T0.1　接通 fork skill 的 spawner

| 项 | 内容 |
|---|---|
| 设计章节 | 差距清单 P0-1 |
| 改动 | `bootstrap.rs:355` 改用 `SkillTool::with_spawner`，传入已构造的 `Arc<AgentSpawner>` 与当前 `session_id` |
| 实现 | 10 行（改动） |
| 测试 | 70 行 |
| 验收 | **测试必须走 bootstrap 装配路径**：从 bootstrap 产出的注册表取 `Skill` 工具，对 `execution_context: fork` 的 skill 调用，断言不返回 "no AgentSpawner is available" |

> 现有 skill 单测全部自建 `SkillTool`，这正是该缺陷逃逸的原因。新测试不得沿用那个模式。

### T0.2　修正 `SpawnTool::describe`

| 项 | 内容 |
|---|---|
| 设计章节 | 差距清单 P2-7 |
| 改动 | `spawn_tool.rs:128` 读 `input["tasks"]` 数组，渲染为 `Spawn: N 个子任务（name1, name2…）` |
| 实现 | 15 行 |
| 测试 | 40 行（0 / 1 / N 个任务 + 缺字段） |
| 验收 | 三任务输入产出含三个名字的描述 |

### T0.3　补 `NO_PROXY` 说明

| 项 | 内容 |
|---|---|
| 改动 | `CONTRIBUTING` 或 `.cargo/config.toml` 说明本地 mock server 需 `NO_PROXY=127.0.0.1,localhost` |
| 实现 | 30 行 |
| 测试 | 20 行（可选：一个检查脚本） |
| 验收 | 新人按文档执行，provider/mcp 测试全绿 |

**阶段小计**：实现 55 / 测试 130 / 合计 185 行 ≈ **0.16 人周**

---

## 四、阶段 1：取消传播与可寻址

**目标**：子获得 id 与生命周期控制。这是后续一切的地基。

### T1.1　`agentrs-types` 契约改造

| 项 | 内容 |
|---|---|
| 设计章节 | §4.1 |
| 改动 | 新建 `agentrs-types/src/subagent.rs`：`SubAgentId` / `SubAgentSpec` / `SubAgentStatus` / `SubAgentResult` / `ForkOverrides` / `Spawner`；删除 `spawner.rs` |
| 实现 | 120 行 |
| 测试 | 145 行（比 1.2） |
| 依赖 | 无 |
| 验收 | `agentrs-types` 单独编译通过；`SubAgentStatus` 的终态判定有测试 |

### T1.2　`SubAgentHandle` 与状态机

| 项 | 内容 |
|---|---|
| 设计章节 | §4.2、§5.1 |
| 改动 | 新建 `subagent/handle.rs` |
| 实现 | 130 行 |
| 测试 | 235 行（比 1.8） |
| 依赖 | T1.1 |
| 验收 | §5.1 的两条不变量有测试：终态互斥；终态后 `join` 必被消费 |

### T1.3　`SubAgentRegistry`

| 项 | 内容 |
|---|---|
| 设计章节 | §2.4、§4.2 |
| 改动 | 新建 `subagent/registry.rs`：`register` / `get` / `list` / `cancel` / `cancel_all` / `drain_turn_usage`；`Drop` 兜底告警 |
| 实现 | 180 行 |
| 测试 | 325 行（比 1.8；含并发注册、并发取消、泄漏回归） |
| 依赖 | T1.2 |
| 验收 | 16 任务并发注册/取消无死锁；`cancel_all` 后 `list()` 为空；未消费 `join` 时 `Drop` 记 `warn` |

### T1.4　取消贯通到子引擎

| 项 | 内容 |
|---|---|
| 设计章节 | §4.3、§5.2 |
| 改动 | `AgentEngine` 增 `subagent_cancel_token()`（`turn_cancel.child_token()`）；子引擎 turn loop 顶部加检查点 C1；`spawn_one`/`spawn_fork` 合并为 `spawn` 并接收 token |
| 实现 | 90 行（含改动 `spawner.rs` 约 60 行） |
| 测试 | 160 行 |
| 依赖 | T1.1、T1.3 |
| 验收 | **改造前必然失败的测试**：传入已 cancel 的 token → 子在一个轮次边界内终止，`status = Cancelled`；父 `cancel_running_tools()` 后 registry 清空 |

### T1.5　`SpawnTool` 接入注册表

| 项 | 内容 |
|---|---|
| 改动 | `spawn_tool.rs` 覆写 `execute_cancellable` 透传 token；`spawn_parallel` 保留 `JoinHandle` 并注册；结果渲染改用 `status` |
| 实现 | 90 行（改动为主） |
| 测试 | 145 行 |
| 依赖 | T1.3、T1.4 |
| 验收 | 现有 `spawn_test.rs` 与 `subagent_collaboration_test.rs` 全绿；新增"取消中途的批次返回部分结果"用例 |

### T1.6　`agentrs-skills` 同步

| 项 | 内容 |
|---|---|
| 改动 | `executor.rs::execute_fork` 适配新签名（`spawn` + token + `status`） |
| 实现 | 40 行（改动） |
| 测试 | 40 行 |
| 依赖 | T1.1 |
| 验收 | `executor_test.rs` 全绿；mock `Spawner` 更新 |

**阶段小计**：实现 650 / 测试 1,050 ≈ 1,700 行；含 §12 预留的 `inbox.rs` 空实现（40 / 40）后 **1,780 行 ≈ 1.51 人周**

---

## 五、阶段 2：上下文与用量

### T2.1　子提示词按能力重建

| 项 | 内容 |
|---|---|
| 设计章节 | §6.2 |
| 改动 | 新建 `subagent/context.rs::build_child_prompt`；`spawn` 不再沿用父 `config.system_prompt` |
| 实现 | 110 行 |
| 测试 | 275 行（比 2.5） |
| 验收 | **改造前必然失败**：父提示词含 "Spawn" 时子的 `request.system` 不含 Spawn/Skill/WebSearch 段，且与 `request.tools` 一致；**且** workspace / shell / AGENTS.md 段未丢失 |

### T2.2　子身份段

| 项 | 内容 |
|---|---|
| 设计章节 | §6.5 |
| 改动 | 固定段落 + `SubAgentIdentity` 参数（当前恒 `None`） |
| 实现 | 60 行 |
| 测试 | 150 行 |
| 验收 | 子提示词含"最后一条文本即返回值""改文件须列出路径"两条；`identity = Some` 时追加身份信息 |

### T2.3　用量归集

| 项 | 内容 |
|---|---|
| 设计章节 | §9.1、§4.3 |
| 改动 | `AgentEngine::record_subagent_usage`；turn 结束/中断时 `drain_turn_usage` |
| 实现 | 70 行 |
| 测试 | 125 行 |
| 验收 | 3 子各消耗已知 usage → 父 `total_usage` 增量等于三者之和；**且 `context_state.context_usage` 不因子的用量变化** |

### T2.4　`SubAgentSink`

| 项 | 内容 |
|---|---|
| 设计章节 | §9.2 |
| 改动 | 新建 sink，替换 `NullSink`；只转发进度类事件 |
| 实现 | 90 行 |
| 测试 | 160 行 |
| 验收 | 子的流式文本不到达父 sink；状态/轮次/token 事件到达 |

### T2.5　上行结构化输出

| 项 | 内容 |
|---|---|
| 设计章节 | §7.2 |
| 改动 | `<subagent id name status>` 渲染；截断作用于 `<result>` 内部 |
| 实现 | 70 行 |
| 测试 | 90 行（含解析器测试防格式漂移） |
| 验收 | 超长结果截断后标签结构完整；多子按请求顺序拼接 |

**阶段小计**：实现 400 / 测试 800 = **1,200 行 ≈ 1.02 人周**

---

## 六、阶段 3：配置化预算

### T3.1　`agentrs-config/src/subagent.rs`

| 项 | 内容 |
|---|---|
| 设计章节 | §10 |
| 改动 | 8 个配置字段 + 默认值 + serde + 级联合并 |
| 实现 | 110 行 |
| 测试 | 130 行 |
| 验收 | 全字段默认值正确；配置文件级联覆盖生效 |

### T3.2　并发闸门

| 项 | 内容 |
|---|---|
| 改动 | `SubAgentRegistry` 接入 `Semaphore` |
| 实现 | 50 行 |
| 测试 | 90 行 |
| 验收 | `max_concurrent = 2` 时 5 子的并发峰值 ≤ 2（计数器 provider 断言） |

### T3.3　深度控制

| 项 | 内容 |
|---|---|
| 设计章节 | §6.1 规则 3 |
| 改动 | `SubAgentSpec.depth` 传递；`depth + 1 < config.depth` 时子的工具集含 `Spawn`；超限返回可操作错误文本 |
| 实现 | 80 行 |
| 测试 | 145 行 |
| 验收 | `depth = 1` 时子无 `Spawn`；`= 2` 时子有而孙无；超限错误文本含配置项名 |

### T3.4　token 预算

| 项 | 内容 |
|---|---|
| 改动 | `turn_output_tokens` 累加与裁决；超限时 `Spawn` 报错 |
| 实现 | 90 行 |
| 测试 | 115 行 |
| 验收 | 预算耗尽后 `Spawn` 返回错误而非静默继续；父 turn 结束后计数归零 |

**阶段小计**：实现 330 / 测试 480 = **810 行 ≈ 0.69 人周**

---

## 七、阶段 4：子会话与续跑

**本阶段最大，且含唯一的存储层改动。**

### T4.1　`SessionManager` 支持子会话

| 项 | 内容 |
|---|---|
| 设计章节 | §6.4 |
| 改动 | `create` 支持填 `forked_from` / `root_id`；`forked_from` 注释改述为"血缘父：fork 或子智能体派生" |
| 实现 | 70 行 |
| 测试 | 130 行 |
| 验收 | 子会话落盘且 `forked_from` 正确；旧会话文件读取不受影响 |

### T4.2　`children()` 反向索引

| 项 | 内容 |
|---|---|
| 改动 | 会话索引增加按 `forked_from` 查询；`SessionIndex` 结构调整 |
| 实现 | 110 行 |
| 测试 | 200 行（比 1.8；含索引重建、损坏索引降级） |
| 验收 | `children(parent)` 返回全部直接子；索引缺失时能从会话文件重建 |

### T4.3　级联删除

| 项 | 内容 |
|---|---|
| 设计章节 | §8 |
| 改动 | 删父会话时递归取消并删子会话 |
| 实现 | 80 行 |
| 测试 | 145 行 |
| 验收 | 三层嵌套下删根会话，全部子孙会话与其运行中任务一并清除 |

### T4.4　会话列表过滤

| 项 | 内容 |
|---|---|
| 设计章节 | §14 Q2 |
| 改动 | `/resume` 与列表默认加 `forked_from IS NONE` 过滤；协议侧暴露"是否含子会话"参数 |
| 实现 | 60 行 |
| 测试 | 90 行 |
| 验收 | 默认列表不含子会话；显式请求时可列出 |

### T4.5　子会话落盘策略

| 项 | 内容 |
|---|---|
| 设计章节 | §14 Q3（**待拍板**） |
| 改动 | `subagent.persist_sessions`；被协议/TUI 持有的子实时写盘，其余终态一次性落盘 |
| 实现 | 90 行 |
| 测试 | 130 行 |
| 验收 | 关闭时不产生子会话文件；开启时终态落盘；`max_sessions` 淘汰覆盖子会话 |

### T4.6　续跑

| 项 | 内容 |
|---|---|
| 设计章节 | §4.1 `resume` |
| 改动 | `Spawn` schema 增 `task_id`；`spawn` 走恢复路径；子 id 记入会话 metadata（§5.4 的压缩幸存要求） |
| 实现 | 130 行 |
| 测试 | 235 行 |
| 验收 | 续跑的第二轮请求含第一轮历史；父 autocompact 后 id 仍可用 |

### T4.7　协议事件

| 项 | 内容 |
|---|---|
| 设计章节 | §11 |
| 改动 | `SubAgentStarted` / `Progress` / `Finished` 三事件 + TUI 渲染 |
| 实现 | 130 行（协议 60 + TUI 70） |
| 测试 | 155 行 |
| 验收 | 事件不含 prompt 与结果正文；TUI 显示子的进度与 token |

**阶段小计**：实现 670 + TUI/协议 340 = 1,010 / 测试 1,660 = **2,670 行 ≈ 2.27 人周**

---

## 八、阶段 5：工具投影与隔离

### T5.1　工具状态外移（**先做技术验证**）

| 项 | 内容 |
|---|---|
| 设计章节 | §14 Q1 |
| 改动 | 把 `ReadTool` 的文件缓存提到显式 `ToolContext`（每引擎一份）；`ExecCommandTool` 同查 |
| 实现 | 160 行 |
| 测试 | 290 行（比 1.8） |
| 触及行数 | 约 400（全部注册点） |
| 验收 | 工具无内部可变状态；子不继承父的"已读文件"记录；并发多子读同一文件无串扰 |

> **本 task 是整个方案唯一需要先做 spike 的点**。若发现某工具确有无法外移的状态，退回"逐工具 `clone_for_child`"方案，成本 +100 行左右。

### T5.2　`ToolRegistry` 改持 `Arc<dyn Tool>`

| 项 | 内容 |
|---|---|
| 改动 | 注册表与全部注册点 |
| 实现 | 80 行 |
| 测试 | 80 行 |
| 触及行数 | 约 250（机械改动） |
| 依赖 | T5.1 |
| 验收 | 全仓编译通过；工具可被多个注册表共享 |

### T5.3　`project_tools` 投影

| 项 | 内容 |
|---|---|
| 设计章节 | §6.1 |
| 改动 | 五步投影规则；`NEVER_INHERITED` 常量表（含排除理由注释） |
| 实现 | 130 行 |
| 测试 | 235 行 |
| 依赖 | T5.2 |
| 验收 | 子获得父的 MCP / ViewImage / ToolSearch；`Spawn` / 计划模式工具被排除；策略过滤后不可扩权 |

### T5.4　只读命令白名单

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.4（`explore` 的前置） |
| 改动 | `ExecCommandTool` 支持只读模式：命令白名单 + 重定向/管道拒绝 |
| 实现 | 130 行 |
| 测试 | 235 行（比 1.8；含绕过尝试用例） |
| 验收 | `ls`/`git status`/`cat` 放行；`rm`/`>`/`>>`/heredoc/`mkdir` 拒绝 |

> 若本 task 延后，`explore` 预制 agent 首版**不给 `ExecCommand`**（见 §4.4.4）。

### T5.5　worktree 隔离（可选）

| 项 | 内容 |
|---|---|
| 改动 | `Spawn` 增 `isolation: "worktree"`；创建/清理；未改动自动删除 |
| 实现 | 200 行 |
| 测试 | 250 行 |
| 验收 | 子在独立 worktree 中工作；无改动时自动清理；Windows CI 通过 |

**阶段小计**：实现 700 / 测试 1,090 = **1,790 行 ≈ 1.52 人周**（T5.5 可选，扣除后 1,340 行 ≈ 1.14 人周）

---

## 九、阶段 6：Agent 定义与预制

### T6.1　`AgentDefinition` 类型

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.2 |
| 实现 | 110 行 |
| 测试 | 130 行 |
| 验收 | 字段默认值；`ForkOverrides` 可合并进定义 |

### T6.2　定义注册表与加载

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.3 |
| 改动 | 三来源加载 + 同名覆盖 + frontmatter 解析（复用 skills 约定） |
| 实现 | 190 行 |
| 测试 | 270 行 |
| 验收 | 项目 > 用户 > 内置；单个文件解析失败仅告警跳过；`builtin_agents = false` 时清空内置 |

### T6.3　定义驱动子的构建

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.5 |
| 改动 | `resolve` → 策略/工具/提示词/模型/预算的接线；优先级链 |
| 实现 | 130 行 |
| 测试 | 235 行 |
| 依赖 | T6.2、T5.3、T2.1 |
| 验收 | **不可扩权不变量有测试**：定义请求父没有的工具时不生效；优先级链四级均有用例 |

### T6.4　内置定义

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.4 |
| 改动 | `general-purpose` / `explore` / `plan` 三份定义 + 各自提示词 |
| 实现 | 180 行（含提示词文本） |
| 测试 | 180 行 |
| 验收 | `explore` 无任何写工具；`plan` 产出含"关键文件"段；`omit_project_rules` 生效 |

### T6.5　`whenToUse` 注入父提示词

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.7 |
| 改动 | `SystemPromptCache` 新增 `subagents` 段；随 `ToolPolicy` invalidate；`Spawn` schema 增 `agent_type` |
| 实现 | 90 行 |
| 测试 | 225 行（比 2.5） |
| 验收 | 策略禁 `Spawn` 时不渲染该段；`hidden` 定义被过滤；段落大小可测 |

### T6.6　hidden agent 收编内部 LLM 调用（可选）

| 项 | 内容 |
|---|---|
| 设计章节 | §4.4.6 |
| 改动 | `summarizer.rs` 与 `ProviderSummarizer` 改走 hidden 定义 |
| 实现 | 80 行 |
| 测试 | 120 行 |
| 验收 | 两处的模型选择走统一配置面；行为不变 |

**阶段小计**：实现 780 / 测试 1,160 = **1,940 行 ≈ 1.65 人周**（T6.6 可选，扣除后 1,740 行 ≈ 1.48 人周）

---

## 十、总规模与排期

### 10.1 三档范围

| 档 | 内容 | 实现 | 测试 | 合计 | 人周 |
|---|---|---|---|---|---|
| **最小可用** | 阶段 0–3 | 1,475 | 2,500 | **3,975** | **3.38** |
| **推荐** | 阶段 0–5（T5.5 可选不做） | 3,145 | 5,250 | **8,395** | **7.15** |
| **完整** | 阶段 0–6 全做 | 3,965 | 6,410 | **10,375** | **8.83** |

按 1,175 行/人周折算；加 5–8% 文档工时后：

| 档 | 人周（含文档） | 1 人 | 2 人 |
|---|---|---|---|
| 最小可用 | 3.6 | 3.6 周 | 2 周 |
| 推荐 | 7.6 | 7.6 周 | 4 周 |
| 完整 | 9.4 | 9.4 周 | 5 周 |

> 2 人并行的折算已考虑阶段间串行依赖（阶段 3 与 4 可并行，其余需排队），因此不是简单除以 2。

### 10.2 与既有估算的对照

| 来源 | 数字 | 说明 |
|---|---|---|
| 本文"推荐"档 | 7.6 人周 | 阶段 0–5 |
| 评估文档 L1 | 5–8 人日 | 对应阶段 0–3 = 3.6 人周 ≈ 18 人日。**评估文档低估了约 2 倍** |
| 评估文档 L2 | 3–4 周 | 对应阶段 4–5 = 3.8 人周。**吻合** |
| Team 方案 Phase 3 | 8–12 人日 | 其中 `spawn_persistent` + inbox drain 属本方案阶段 1/4，不应记在 Team 账上 |

**L1 低估的原因**：评估文档按"改动点数量"估，未计测试。本文按 1.62 的测试比展开后，阶段 0–3 的 2,500 行测试是主要增量。**以本文数字为准。**

### 10.3 里程碑

| 里程碑 | 完成 task | 可交付的用户可见变化 |
|---|---|---|
| M1（第 1 周末） | 阶段 0 + T1.1–T1.3 | 无用户可见变化，但 fork skill 恢复可用 |
| M2（第 2 周末） | 阶段 1 完 | **Ctrl-C 真正终止子智能体** |
| M3（第 3 周末） | 阶段 2 完 | 每子每请求省约 420 token；会话用量准确 |
| M4（第 4 周末） | 阶段 3 完 | 并发/深度/预算可配 |
| M5（第 6 周末） | 阶段 4 完 | 子可见、可续跑、可回看 |
| M6（第 8 周末） | 阶段 5 完 | 子可用 MCP/Web；`explore` 的只读基础就绪 |
| M7（第 9 周末） | 阶段 6 完 | `Spawn` 支持 `agent_type`，预制三个 agent |

---

## 十一、风险与估算置信度

### 11.1 置信度分级

| 阶段 | 置信度 | 理由 |
|---|---|---|
| 0–3 | **高** | 改动局部，无存储/无并发新机制；已有实测证据定位问题 |
| 4 | **中** | 触及 `SessionManager` 与索引，存在"旧会话兼容"的未知；T4.2 的索引重建可能超估 |
| 5 | **低** | T5.1 的工具状态外移是唯一 spike 点，可能发现无法外移的状态 |
| 6 | **中高** | 结构清晰，但内置提示词的行数取决于写多细 |

### 11.2 主要风险

| ID | 风险 | 影响 | 缓解 |
|---|---|---|---|
| E1 | **费率取自 aionrs，非本团队实测** | 全部人周数字等比例偏移 | 阶段 0–1 完成后用真实产出反推，重算后续（与校准报告的建议一致） |
| E2 | T5.1 发现工具状态无法外移 | +100 行，且需退回逐工具方案 | 阶段 5 开工前先做半天 spike |
| E3 | T4.2 会话索引改动波及面超预期 | +200~300 行 | 先只做内存索引 + 启动时扫描，暂不改索引文件格式 |
| E4 | 内置 agent 提示词反复调优 | 行数不涨但工时涨 | 首版按对标实现直译，不做本地化优化 |
| E5 | 阶段 5/6 与 Team 线争抢同一批文件 | 合并冲突 | Team 线必须排在阶段 4 之后启动 |

### 11.3 建议的校准点

**阶段 1 结束时做一次费率校准**：用 T1.1–T1.6 的真实产出（实现行 / 测试行 / 实际人日）反推本团队费率与测试比，重算阶段 2–6。届时本文 §10 的全部数字应被替换——这与 `uworker-规模估算与排期校准报告.md` §3 的做法一致。
