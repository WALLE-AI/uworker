[package]
name = "agentrs-memory"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "AgentRS: 记忆选择 —— 只在候选里挑，不确定时少选"

[dependencies]
agentrs-contracts = { workspace = true }
agentrs-prompts = { path = "../agentrs-prompts" }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }
agentrs-provider = { path = "../agentrs-provider" }
agentrs-subagents = { path = "../agentrs-subagents" }
agentrs-types = { workspace = true }

[lints]
workspace = true
